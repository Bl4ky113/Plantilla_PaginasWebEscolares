module.exports = {
    validarString = string=>{
        if(string.indexOf('!') > 0 ||
        string.indexOf('"') > 0 ||
        string.indexOf('#') > 0 ||
        string.indexOf('$') > 0 ||
        string.indexOf('%') > 0 ||
        string.indexOf('&') > 0 ||
        string.indexOf('/') > 0 ||
        string.indexOf('(') > 0 ||
        string.indexOf(')') > 0 ||
        string.indexOf('=') > 0 ||
        string.indexOf('?') > 0 ||
        string.indexOf('¡') > 0 ||
        string.indexOf("'") > 0 ||
        string.indexOf('¿') > 0 ||
        string.indexOf('|') > 0 ||
        string.indexOf('*') > 0 ||
        string.indexOf('*') > 0 ){
            //aslk
        }
    }
}